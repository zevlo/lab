# Script entry point is solid

Completed Lesson 0017 with 3/3 on the quiz and a correct from-memory script: `status_line` returns a string, `main` owns argv + print/exit, and `if __name__ == "__main__":` calls `main()`.

## Evidence

User pasted their script after completing the lesson. Structure matches the exercise spec exactly.

## Notes

User needed secondary reading beyond the lesson to fully lock the concept (run vs import / what `__name__` is). The pattern itself is fluent; the *why* took an extra pass.

## Implications

- Interview-shaped script structure is now available: functions + `main` + guard.
- Abstract “how Python loads a file” ideas may need a slower explainer or a tiny live demo next time they appear.
- Good next topics: light classes enough to read others’ code, or a small combine/capstone that uses write + entry point + an earlier skill.
