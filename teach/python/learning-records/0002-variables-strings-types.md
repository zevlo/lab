# Variables, f-strings, and the type boundary are solid

Completed Lesson 0001 with 3/3 on the quiz and the terminal exercise done, including deliberately triggering a TypeError. The user can assign variables, interpolate with f-strings, and convert between str and int with `int()`/`str()`.

## Evidence

Self-reported quiz 3/3 and completion of the interactive-shell exercise (f-string with two variables, expression inside braces, reading a TypeError).

## Implications

- Floor is set: don't re-teach assignment, print, or f-strings.
- Next in sequence: lists + for loops (bridge from bash `for f in *`), then dicts.
- The user responded well to the quiz + type-it-in-terminal format; keep that structure.
