# Lists, for loops, and indentation-as-syntax are solid

Completed Lesson 0002 with 3/3 on the quiz and a correct from-memory script: list literal, `.append()`, `for` loop with f-string body, and `len()` inside an f-string after the loop. Block indentation was handled correctly without prompting.

## Evidence

User pasted their actual script — it appends before the loop, iterates cleanly, and the post-loop print is correctly dedented.

## Implications

- Fundamentals floor is now: variables, f-strings, type conversion, lists, for/range, indentation blocks.
- Next: dictionaries, then conditionals (`if`), then functions.
- User is progressing fast (two lessons in three days, clean output both times) — can slightly increase exercise difficulty.
