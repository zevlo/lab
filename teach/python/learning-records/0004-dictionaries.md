# Dictionaries are solid

Completed Lesson 0003 with 3/3 on the quiz and a correct from-memory script: dict literal, adding a key by assignment, `.items()` unpacking in a for loop, and `.get()` with a fallback. Output matched the expected output exactly.

## Evidence

User pasted their script. Only issue was cosmetic (a space before `[` in `inventory ["prometheus"]` — PEP 8 style, pointed out in feedback).

## Implications

- Floor now includes dicts: literal syntax, lookup, `.get()`, `.items()` iteration.
- Next: conditionals (if/elif/else), then functions, then file reading.
- Difficulty calibration: user asked to HOLD difficulty steady (see NOTES.md) — three guided exercise steps with expected output is the right level; don't escalate.
