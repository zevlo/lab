# Classes sat past ZPD — need a slower pass

Completed Lesson 0018 exercise with a correct script (`Host`, `__init__`, `summary`, `main`, entry guard). User reported the topic is important but beyond current ZPD — missing prerequisite mental model for objects/instances/`self`, not a typing failure.

## Evidence

Pasted code matches the exercise. `sys.exit(1)` is equivalent to `raise SystemExit(1)`.

## Implications

- Do not treat “classes enough to read” as locked yet. Plan a remediation/retrieval lesson that builds the object model first (instance vs blueprint, attributes as data on an object, methods as functions with automatic `self`) before any new OOP surface.
- Hold difficulty: next class-related lesson should be narrower and more concrete than 0018, not a step up.
- Capstone/combine lessons can wait until the object model feels familiar.
