# Conditionals are solid, including branch ordering

Completed Lesson 0004 with 3/3 on the quiz and a correct from-memory script: if/elif/else inside a loop, `==` comparison, `and` to combine conditions — and crucially, the user ordered the specific check (`sshd` maintenance) before the general one (`down` alert) without being shown how. They also formatted a dict literal across multiple lines unprompted.

## Evidence

User pasted their script; the elif ordering was the deliberate trap in the exercise and they got it right.

## Implications

- Floor now includes: variables, f-strings, types, lists, for/range, dicts, if/elif/else, and/or, nesting (if inside for).
- Fundamentals remaining: functions (next), then file reading.
- After that the DevOps layer opens: log parsing, subprocess, JSON/APIs.
