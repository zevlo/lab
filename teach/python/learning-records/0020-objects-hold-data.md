# Objects-hold-data model is landing

Completed Lesson 0019 remediation with 3/3 on the quiz. User can define `Host` with `__init__` storing `self.name` and `self.status` — the blueprint → instance → attributes slice that Lesson 0018 skipped past.

## Evidence

Quiz 3/3. Pasted `__init__` matches the remediation shape (no methods required for this lesson’s win condition).

## Implications

- Treat “each instance is its own bag of data” as the new floor for OOP.
- Next class lesson can reintroduce methods (`summary(self)`) as “functions that read this bag,” still without escalating to inheritance or design patterns.
- Revisit Lesson 0018’s full script only after methods feel like a thin layer on top of attributes.
