apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: read-secrets
  namespace: rbac-lab
subjects:
- kind: ServiceAccount
  # Break: typo — Role exists, but this SA name is not who the pod uses
  name: secret-reedr
  namespace: rbac-lab
roleRef:
  kind: Role
  name: secret-reader
  apiGroup: rbac.authorization.k8s.io
