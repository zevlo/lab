apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: api-tls
  namespace: certmanager-lab
spec:
  secretName: api-tls
  issuerRef:
    name: lab-ca
    kind: Issuer
  dnsNames:
  - api.certlab.k8s.orb.local
