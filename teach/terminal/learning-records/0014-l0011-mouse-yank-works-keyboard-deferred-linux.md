# L0011: mouse yank works; keyboard Meta deferred to Linux

User confirmed copy-mode works via mouse selection on Mac (`mouse on` → drag → `copy-pipe-and-cancel`). Will retry keyboard yank (`C-w` / Option-as-Meta) on a Linux machine later. Quiz was already 4/4.

**Implications:** Buffer paste concept is usable. Do not block Phase 3 on Mac Meta. Next lesson: synchronize-panes (mission multi-host). Remind Meta/C-w when they report from Linux.
