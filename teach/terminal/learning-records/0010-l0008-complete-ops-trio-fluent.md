# L0008 complete 4/4; ops trio fluent

User finished Lesson 0008 quiz 4/4. Self-report: `pgrep`, `kill` (and the ops loop) felt good — new floor for process find/signal and `curl -i` with interleaved `cut`.

**Implications:** Do not re-teach the ops trio unless spaced retrieval later shows decay. Next slice from mission gaps still open: redirect mastery (`2>&1` / `tee` under failure), `dig` + remote `ss`, more `cut` spacing, or deferred tmux copy-mode (Mac: buffer paste portable; Option-w ≠ macOS clipboard).
