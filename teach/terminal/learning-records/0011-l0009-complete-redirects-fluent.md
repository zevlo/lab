# L0009 complete 4/4; redirects fluent

User finished Lesson 0009 quiz 4/4 with no confusion. Stdout/stderr, `>` vs `>>`, merge order (`>f 2>&1`), and `tee` are a new floor — the five-family shell pareto from L0007 is now drilled end-to-end (map → filters → ops → redirect).

**Implications:** Do not re-teach redirection unless spaced retrieval shows decay. Mission gaps still open for next slice: `dig` + remote `ss`, tmux copy-mode (Mac buffer caveats), or spaced retrieval on `cut` / ops.
