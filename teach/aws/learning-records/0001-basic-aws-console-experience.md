# Basic AWS console experience established

The learner has used the AWS console for a few simple tasks. Future lessons can skip basic console navigation, but they should still introduce each service's mental model before asking the learner to configure it.
