# Public subnet and direct IPv4 reachability distinguished

The learner correctly identified that a subnet is public when its associated route table points to an internet gateway. They also separated the EC2 instance requirements: a public IPv4 address and an inbound security group rule. Future lessons can assume this network-path model and add EC2 runtime checks.
