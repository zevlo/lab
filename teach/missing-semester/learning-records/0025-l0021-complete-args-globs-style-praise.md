# L0021 complete 4/4 — structure/style praised

User finished Lesson 0021 quiz 4/4. Explicit positive feedback on lesson structure and style — keep MS-era pattern (why → Knowledge → drill with hidden answers → shuffled quiz → ask-the-teacher; no pedagogy-meta in lesson copy).

**Implications:** Arguments & globs floor set (`$0`/`$@`/`$#`, flags, shell expands `*`/`?`/`{}`, quotes freeze globs). Do not re-teach unless retrieval decay shows up. Next CLE slice when requested: env vars / `export`, or return codes `&&` `||` / `$?`.
