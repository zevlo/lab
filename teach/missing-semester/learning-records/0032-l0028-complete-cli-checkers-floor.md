# L0028 complete 4/4; CLI checkers floor set

User finished Lesson 0028 quiz 4/4. No friction reported on `py_compile` / `gofmt` + `go vet` / `mypy` install or error reading.

**Implications:** LSP-vs-CLI split + exit-status reuse (L0023) landed. `python3 -m py_compile` / `gofmt -w` / `go vet ./...` / `mypy` floor set — do not re-teach unless retrieval decay shows. MS lecture 3 (Dev Environment) terminal-track slices done; AI form factors (autocomplete / inline chat / agents) remain in lecture 3.

**Next when requested:** AI form factors, more vim practice, or MS lecture 4 (Debugging and Profiling) — user picking.
