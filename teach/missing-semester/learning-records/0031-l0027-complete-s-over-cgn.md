# L0027 complete 6/6; prefers :s over cgn; asks visual yank

User finished Lesson 0027 quiz 6/6. Questions: simplest visual yank/paste; advantages of `:s` vs `cgn` (currently prefers substitute).

**Implications:** Motions practice pass done. Preference for `:%s` is valid for bulk identical replaces — do not shame it. Keep `cgn`+`.` as the surgical/selective tool. Visual yank simplest path: `V` (or `v`/`Ctrl-v`) → move → `y` → move → `p`/`P` (unnamed register); named register only when needed (`"ay` / `"ap`).

**Next when requested:** another vim pass, language servers / CLI checkers, or AI form factors.
