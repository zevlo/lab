# L0015 complete; MS nav floor OK

User finished Lesson 0015 (Missing Semester lecture 1 — nav / PATH / `man` slice) with no issues.

**Implications:** Absolute vs relative paths, `cd` builtin, PATH/`which`, and `man` habit are a usable floor. Continue MS lecture 1 with pipes + core filters (`cat`/`head`/`tail`/`grep`/`sort`/`uniq`) under high-rep pedagogy — do not jump to `find`/`sed`/`awk`/scripting yet. Keep fancy MS alternate CLIs out of scope.
