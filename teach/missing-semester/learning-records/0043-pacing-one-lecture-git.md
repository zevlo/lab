# Pacing correction: one lecture → one lesson; Git now

User (after L0038 4/4): too many lessons per Missing Semester lecture. Hit the important topics and cover the **exercise section**. Should already be on Version Control and Git.

**Lecture 4 close:** L0031–0038 stand. L0039 (massif) withdrawn — notes-only, not an exercise. `hyperfine` left unnamed-as-a-lesson (MS example is `fd` vs `find`; fancy-CLI constraint). `rr` still named (no PMU).

**Policy from here:** one MS lecture → one HTML lesson (exercises in the same file; spread sittings if needed). No more subsection slices.

**Next:** L0040 Version Control and Git (data model + CLI map + the nine MS exercises).
