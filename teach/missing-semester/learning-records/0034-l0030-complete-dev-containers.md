# L0030 complete 4/4; dev containers floor set

User finished Lesson 0030 quiz 4/4. No friction reported on the three-noun split (image / container / dev container), the `docker` CLI core, the `-it` / `--rm` / `-v` / `-w` / `-p` flags, or the `devcontainer.json`-as-versioned-config point.

**Implications:** MS lecture 3 (Dev Environment) is fully done — terminal-workflow map (L0026), LSP + CLI checkers (L0028), AI form factors (L0029), and dev containers (L0030). The image-vs-container-vs-devcontainer distinction, the `docker` verbs, and the L0028-checkers-baked-into-the-image-as-portable-gate reuse all landed. Do not re-teach unless retrieval decay shows.

**Mission-constraint posture held:** IDE/dev-container extension path named, not expanded (same as Zed in L0029, kitty/starship in L0025). The `docker` CLI is the curriculum; the `devcontainer` CLI framed as an optional one-off install like `jq` (L0020) / `mypy` (L0028). Container build depth (multi-stage, registries, CI) deferred to MS Shipping Code; compose/k8s still out of scope.

**Next when requested (now taken):** MS lecture 4 (Debugging and Profiling) — L0031 in flight, sliced to the foundational debugging half (golden rule + printf/logging + `pdb` + methodology).
