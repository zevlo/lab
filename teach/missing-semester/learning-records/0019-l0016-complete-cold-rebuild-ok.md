# L0016 complete 4/4; cold rebuild + spacing OK

User finished Lesson 0016 quiz 4/4. Drills effective. Cold rebuild of `sort | uniq -c | sort -nr | head` succeeded without hints, including optional step 8 from `/tmp` in a fresh context.

**Implications:** Core filters + pipe composition are a new floor — do not re-teach unless retrieval decay shows up. Continue MS lecture 1 with `find` / light `sed` / `awk` (high-rep, macOS `sed -i ''` caveat, no fancy CLIs, defer deep scripting).
