# L0007 complete 4/4; cut brand-new; ops still soft

User finished Lesson 0007 quiz 4/4. The five-family map and the `sort | uniq -c` adjacency rule landed. Self-report: first time ever using `cut`; not solid on `pgrep` / `kill` / `curl`.

**Implications:** Lesson 0008 drills the ops trio on a local process you control, and interleaves `cut` (delimiter + field) for storage strength. `dig` / `ss` stay deferred. Redirect mastery and tmux copy-mode wait.
