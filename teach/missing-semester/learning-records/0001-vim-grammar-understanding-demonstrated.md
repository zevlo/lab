# User demonstrated vim grammar understanding

In the Lesson 0001 drill, the user completed all six tasks on the broken YAML config and scored 4/4 on the retrieval quiz. They singled out `ciw` as the most natural operator + text-object combo — strong signal that the `[count] operator motion` shape has clicked at the sentence level, not just as memorized keystrokes.

Operator, motion, and text-object are now promoted into [GLOSSARY.md](../GLOSSARY.md).

**Implications**: Lesson 0002 can move past grammar basics into navigation tightening (`f`/`F`/`t`/`T`, `;`/`,`, `%`, marks) without re-teaching the grammar. Visual/block mode, registers, and macros remain ahead.
