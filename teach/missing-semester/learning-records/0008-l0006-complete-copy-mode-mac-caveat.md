# L0006 complete 4/4; hierarchy fluent; copy-mode Mac caveat for L0007

User finished Lesson 0006 with quiz 4/4. Sessions, windows, panes, attach, detach, and splits all felt good — hierarchy is a new floor for Phase 3.

Constraints for a future copy-mode / paste lesson (user stated explicitly): on this Mac, `C-b [` + `Option-w` fills the **tmux paste buffer** but does **not** reach the macOS clipboard. Mouse / Ghostty selection is the local Mac path; teach stock buffer yank (`C-b ]`) as the portable default, and do not claim Option-w → Notes works on their stack.

Also surfaced: user is unclear how "history" layers (bash ↑ / tmux scrollback / Ghostty scrollback) relate — answer in-session; optional micro-topic later if it keeps biting.
