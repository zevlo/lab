# L0018 complete 4/4; cold rebuild a/b/c took two passes

User finished Lesson 0018 quiz 4/4. Cold rebuilds (a if/test, b for-loop, c shebang + `set -euo pipefail` + `chmod +x` for `./check.sh`) needed two passes to land correctly — called out as good repetition.

**Implications:** Light bash floor set (`name=value`, `[ -f ]`, `for`, shebang, strict `set`, `$1`, execute bit). Do not re-teach unless retrieval decay shows up. MS lecture 1 (shell) complete. Next when requested: MS lecture 2 — Command-line Environment.
